{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null && $userId === auth.uid",
        ".write": "auth != null && $userId === auth.uid",

        "apiKeys": {
          ".read": false,
          ".write": "auth != null && $userId === auth.uid"
        }
      }
    }
  }
}